google-adk
google-genai
# mcp must stay below 2.0: ADK 2.7.1 imports mcp.shared.session.ProgressFnT,
# which mcp 2.0 removed. Installing mcp>=2.0 breaks the agent at import time.
mcp<2
mcp-clickhouse
clickhouse-connect
python-dotenv
fastapi
python-multipart
uvicorn
google-api-python-client
# Only needed for the Vertex ingest path: Vertex has no Files API, so
# clips reach it as gs:// objects. The Developer-key path needs none of this.
google-cloud-storage
google-auth
google-auth-httplib2
google-auth-oauthlib

# Kept here rather than a separate dev file so that the README's install
# line is enough to run the test suite the README tells you to run.
pytest
